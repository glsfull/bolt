# admin-settings

Edge Function boundary for protected administration.

Responsibilities:
- require the `admin` role;
- manage AI/OCR provider settings and feature flags;
- keep secrets server-side;
- write audit records for every change.
