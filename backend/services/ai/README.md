# AI service boundary

Server-only adapters for symptom checks, marker normalization and report
recommendations live here. Mobile code calls Edge Functions rather than reading
AI provider keys directly.
